using UnityEngine;
using System.Collections;

public class vactor3test : MonoBehaviour {

	// Use this for initialization
	void Start () {
//		Vector3 v = new Vector3 ();
//		float x = v.x;
//		float y = v.y;
//		float z = v.z;
		// 获取v的单位向量
		// v的长度会变为1 但是方向不变
//		v.Normalize ();
		//返回v方向的单位向量 ，但是v本身不会发生变化
//		Vector3 vzero = v.Normalize; 
		//获取v的长度
//		float l = v.magnitude;

		// x=0 y=1 z=0;
//		Vector3 u = Vector3.up; //表示世界坐标系中y轴正方向上的单位向量
//		Vector3 d = Vector3.down; // y轴负方向上的单位向量
//		Vector3 r = Vector3.right; // x轴正方向的单位向量
//		Vector3 lef = Vector3.left; //x轴负方向；
//		Vector3 f = Vector3.forward; //  z轴正方向
//		Vector3 b = Vector3.back; // z轴负方向
//
//		Vector3.zero; // 原点

		// 求两个向量的夹角
//		Vector3 v1 = new Vector3 (1.4f, 6f, 7.2f);
//		Vector3 v2 = new Vector3 (2f, 6.5f, 12.3f);
//		float angle = Vector3.Angle (v1, v2);

		// 获取两个点的距离
//		Vector3 pos1 = new Vector3 (4f, 2f, 11f);
//		Vector3 pos2 = new Vector3 (2f, 13.6f, 8f);
//		float dis = Vector3.Distance (pos1, pos2);

		// 向量点乘 
//		float dd = Vector3.Dot (v1, v2);
		// 向量叉乘
//		float xx = Vector3.Cross (v1, v2);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
